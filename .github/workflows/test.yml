name: Verify AWS Infra

on:
  push:
  pull_request:

jobs:
  verify-infrastructure:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Verify CDK infrastructure
        run: npx cdk synth
        env:
          CDK_DEFAULT_ACCOUNT: '730335381248'  # Dummy account for synth
          CDK_DEFAULT_REGION: 'ap-northeast-1'      # Dummy region for synth
